import { EngToNepNumberPipe } from './eng-to-nep-number.pipe';

describe('EngToNepNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new EngToNepNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
