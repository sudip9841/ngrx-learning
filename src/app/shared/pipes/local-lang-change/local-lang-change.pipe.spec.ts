import { LocalLangChangePipe } from './local-lang-change.pipe';

describe('LocalLangChangePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalLangChangePipe();
    expect(pipe).toBeTruthy();
  });
});
